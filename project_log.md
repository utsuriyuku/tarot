# 塔罗牌AI解读工具 - 项目开发日志

## 项目愿景
制作一个具备优秀交互体验和视觉效果（如Three.js粒子特效）的塔罗牌抽取与解读工具，并接入大模型（LLM）进行动态的解牌分析。

## 开发阶段记录
### 第一轮讨论 (2026-05-12)
- **初始需求确定**：
  - 核心功能：塔罗牌抽取、大模型解牌分析。
  - 视觉交互：强调前端表现，计划引入 Three.js 等方案实现粒子特效与沉浸感。
  - 项目管理：在工作区维护Markdown进度日志，并保持多轮对话总结。
- **当前进度**：
  - 建立项目日志文档。
  - 等待与用户确认前端技术栈与整体架构框架。

### 第二轮讨论 (2026-05-12)
- **系统设定升华**：
  - 确定项目的哲学内核：“时间本身不存在”，“未来决定现在”（类似量子物理中的逆因果律或波函数坍缩）。
  - 已将该设定具象化为 `system_prompt.md` 并将其暴露在项目根目录，方便用户随时调优。
- **基础设施建设**：
  - 创建了完善的项目说明 `README.md`。
  - 准备采用 React + Vite + React-Three-Fiber 作为前端脚手架建立基础工程。

### 第三轮讨论 (2026-05-12)
- **UI交互与体验优化**：
  - 明确“页面是美丽的表皮，交互是用户的眼睛”的设计导向。前端组件必须兼顾“美感”和“可理解性”。
  - 解答了开发层面的“配置化（JSON）”疑问：向用户阐明 JSON 是开发代码组织数据的形式（维护78张卡牌），不需要用户自己写代码。
  - **实现 API 挂载节点（配置弹窗）**：引入毛玻璃质感的前端遮罩面板，内敛且优雅的输入框（API URL、API Key、Model），状态存储到 `localStorage` 中。文案包装为“观测节点连接”，融入世界观场景。

### 第四轮讨论 (2026-05-12)
- **核心塔罗业务流闭环**：
  - 创建了 `tarotData.ts` 数据集，定义了核心卡面和量子视角的对应释义（目前已包含愚者、魔术师、命运之轮等大阿卡纳牌用作测试）。
  - 创建了 `llm.ts` 请求服务，集成我们设定好的 System Prompt，组装用户抽牌数据的 payload 发往 OpenAI / 兼容的大模型 API。
  - 完善了 `TarotCard.tsx`：加上了逆位判定，当卡牌被判定为逆位时，翻转动画会在Z轴上额外旋转180度，以视觉化展示“逆位”。
  - 引入了 `InterpretationPanel.tsx`（解读面板）：点击中间的卡牌时，标题会逐渐隐退，右侧优雅滑出一个采用赛博/克苏鲁神秘感排版的解牌框，呈现“打字机 (Typewriter)”的文字喷吐表现效果。如果并未配好 API，它会优雅地引导用于点击“终端配置”。

### 第五轮讨论 (2026-05-12)
- **卡面贴图加载支持**：处理了待办[A. 真实图片材质]，并利用 Vite / ThreeJS TextureLoader 技术配合 React Suspense 实现。
- **意念交互前置**：处理了待办[B. 抽牌前输入]。编写了底部输入框。目前业务链路调整为：必须要用户先凝神打出/写出“执念或疑团”，确认锁定意念后，画面会从幽暗之中微微变亮，3D卡牌才被解锁，允许抽选。这个交互将用户心念作为真正连接 AI 的桥梁。
- **扩充 78 张牌库**：完成了待办[C. 扩充完整78张牌JSON数据]。编写了一个 Node.js 脚本一键生成了包含 22 张大阿卡纳（带有维基真实图照源）和 56 张小阿卡纳的大型数据表 `tarotData.ts`。项目彻底成型。
- **全球公网发布**：使用 GitHub Actions 配置了全自动流水线（CI/CD），通过云渲染直接向 GitHub Pages 投放静态结果。至此整个项目架构完备，支持后续敏捷更迭。

### 第六轮讨论 (2026-05-12)
- **Pages 部署故障定位**：确认 GitHub Actions 红叉的根因不是子路径本身，而是工作流固定使用了 Node 18；当前项目依赖的 Vite 8 与 @vitejs/plugin-react 明确要求 Node `^20.19.0 || >=22.12.0`。
- **修复措施**：将 `.github/workflows/deploy.yml` 中的 Node 版本升级到 22，并在本地重新验证 `npm run build` 已成功通过。当前等待 GitHub 重新执行最新 workflow。
- **样式管线补齐**：进一步确认 Tailwind 样式此前并未真正接入构建流程。已补装 `tailwindcss`、`postcss`、`autoprefixer`，新增 `web-app/postcss.config.js`，并验证构建产物中 CSS 体积恢复正常，页面样式可随部署一起生效。

### 第七轮讨论 (2026-05-12)
- **页面观感重构**：针对线上页面“像未完成工具页”的问题，重排了首页视觉层级。现在界面被拆成信息层、粒子舞台层、交互层三部分，避免标题、输入框、解牌面板和配置弹窗互相抢焦点。
- **交互文案与面板优化**：重写了问题输入条、右侧解牌面板和模型配置弹窗，让按钮、标签、说明文案更接近成品而不是默认浏览器控件。
- **全局基底优化**：补上字体、背景渐变、滚动条、选区和舞台氛围样式，并收敛粒子参数，减少“散点噪声感”。本地构建已再次通过。

### 第八轮讨论 (2026-05-12)
- **待办 1 完成：暗金深墨纸张质感**。将页面主视觉从冷色科幻玻璃风整体切换到暖金、深墨、旧纸纤维质感：包含全局背景、纸张纹理、顶部入口、左侧信息卡、底部问题输入条、设置弹窗、右侧解牌面板，以及粒子与卡牌补光的统一暖色化。
- **验证结果**：本地 `npm run build` 通过，无新增类型或构建错误，可继续进入下一项动效升级。

### 第九轮讨论 (2026-05-12)
- **待办 2 完成：卡牌辉光入场动画**。将卡牌交互拆成三段：问题锁定后的“入场浮现”、鼠标悬停时的“辉光增强”、点击抽牌瞬间的“爆发式坍缩闪光”。实现方式基于 `TarotCard.tsx` 中的外层舞台组、双层辉光平面、符文环与动态点光强度联动。
- **触发机制**：卡牌入场不再无条件存在，而是跟随 `isQuestionLocked` 进入仪式状态；只有当用户先提交问题，卡牌才会从下方浮现并进入可点击阶段。
- **验证结果**：本地 `npm run build` 再次通过，无新增 TS/构建错误。

### 第十轮讨论 (2026-05-12)
- **待办 3 完成：解牌面板杂志排版**。将原本的工具型侧边栏改为更接近刊物页面的结构：顶部题头与副标题、左侧牌面注释栏、右侧正文稿件区、下方整页式操作脚注。
- **阅读结构升级**：对 LLM 返回文本按段落切分，首段采用大字号引言与首字下沉效果，后续段落使用带竖线的正文样式，保留打字机逐字显现逻辑。
- **验证结果**：本地 `npm run build` 通过，当前三项视觉待办全部完成。

### 第十一轮讨论 (2026-05-12)
- **新阶段待办 1 完成：API 配置体验升级**。配置面板不再只是保存本地参数，而是具备了接口地址/密钥/模型的即时校验、推荐模型预设、测试连接按钮，以及结构化的成功/失败反馈。
- **请求服务增强**：在 `web-app/src/services/llm.ts` 中补充了配置校验、接口地址标准化、以及连接测试逻辑。优先尝试 `/models` 检查认证与模型可见性，若服务不支持则回退到最小化 `chat/completions` 请求。
- **验证结果**：本地 `npm run build` 已通过，配置面板可继续作为后续业务层扩展的稳定入口。

### 第十二轮讨论 (2026-05-12)
- **新阶段待办 2 完成：多张抽牌与牌阵**。主流程已从单牌抽取切换为可选牌阵模式，当前支持“单牌切片”“三相展开”“五镜牌阵”三种结构；用户锁定问题后，需要按顺序点击各张牌，直到整组牌全部坍缩。
- **主容器重构完成**：清理了 `web-app/src/App.tsx` 中旧单牌逻辑与新牌阵逻辑混杂造成的语法损坏，改为单一、干净的 spread 版本；同时接通了牌阵切换、逐张抽牌、整组完成后再统一请求大模型综合解读的状态链路。
- **配套能力接线完成**：`spreads.ts` 提供牌阵定义与 3D 布局；`tarotData.ts` 新增不重复多抽 helper；`llm.ts` 新增多牌整体解读入口；`TarotCard.tsx` 与 `InterpretationPanel.tsx` 均已适配多牌显示。
- **验证结果**：本地 `npm run build` 再次通过。当前下一阶段可继续进入“按牌位组织解读”。

### 第十三轮讨论 (2026-05-13)
- **新阶段待办 3 完成：按牌位组织解读**。牌阵解读不再只是整段综合描述，而是改为“总体气候 → 各牌位分节 → 收束”的固定稿件结构。
- **生成层调整**：在 `web-app/src/services/llm.ts` 中收紧多牌 prompt，要求模型严格输出带标题的分节文本，并强制覆盖当前牌阵中的所有牌位名。
- **呈现层调整**：在 `web-app/src/components/InterpretationPanel.tsx` 中新增结构解析逻辑，右侧面板现在会把“总体气候”“各牌位说明”“收束建议”分别渲染；若模型临时未按格式返回，则仍会退回原有段落排版，不至于面板空白。
- **文案同步**：更新 `web-app/src/App.tsx` 中的状态提示，使其与“按牌位展开解读”的新流程一致。
- **验证结果**：本地 `npm run build` 通过。至此本轮牌阵体验已从抽牌、整组解读推进到按位结构化解读。

### 第十四轮讨论 (2026-05-13)
- **部署尝试结果**：在 `web-app` 目录重新执行 `npm run deploy`，本地 `gh-pages -d dist` 返回 `Published`，说明静态产物成功发布到 gh-pages 流程。
- **线上校验结果**：直接抓取 `https://utsuriyuku.github.io/tarot/` 后，页面仍返回旧版 HTML，引用的资源仍是旧哈希 `index-yboJgVis.js` / `index-CWdygvuN.css`，未切到本轮最新构建产物。
- **当前判断**：线上页面目前没有直接读取这次本地 `gh-pages` 发布结果，更可能仍由既有的 GitHub Pages / Actions 主分支流程控制。因此，如果要把当前改动真正推到线上，需要把源码变更提交并推送到远端主分支，或在 GitHub 侧改动 Pages 来源配置。
- **主分支发布已执行**：随后将当前改动提交为 `a01218e` 并推送到 `main`，远端确认仓库已迁移到 `https://github.com/utsuriyuku/tarot.git`，同时触发了 GitHub Actions 中的 `Deploy to GitHub Pages #12`。
- **最终线上验收**：再次抓取 `https://utsuriyuku.github.io/tarot/`，已看到新版页面文案，包括“Spread Selection”“单牌切片 / 三相展开 / 五镜牌阵”“01 锁定问题 · 02 依次翻牌 · 03 查看分位解读”，说明本轮版本已经对外可见。

### 第十五轮讨论 (2026-05-13)
- **新阶段待办 4 完成：优化移动端牌阵**。修复了移动端主舞台仍按桌面策略垂直居中导致的顶部裁切问题；在手机宽度下，页面不再把整块舞台向上挤出视口。
- **布局层调整**：在 `web-app/src/App.tsx` 中加入 viewport 宽度监听，移动端改为顶部对齐布局，并将舞台高度、信息卡尺寸、底部问题输入区和提示胶囊统一收紧为小屏尺寸。
- **牌阵层调整**：为 `single / triad / fivefold` 三种牌阵补上移动端专用的 3D 坐标、缩放和相机参数，避免沿用桌面坐标导致牌位在窄屏上过散或互相挤压。
- **面板层调整**：同步压缩 `QuestionInput.tsx` 与 `InterpretationPanel.tsx` 的移动端宽度、标题字号与滚动区域高度，让抽牌前后两种状态都能在小屏下正常阅读与操作。
- **验证结果**：本地 `npm run build` 通过。浏览器工具无法稳定切换到真正的手机视口尺寸，但当前修复已覆盖此前导致移动端裁切的主因。

### 第十六轮讨论 (2026-05-13)
- **新阶段待办 5 完成：压缩首屏包体积**。首屏不再同步加载全部交互后代码，而是改为页面框架先到、重资产按需进入。
- **按需拆分策略**：在 `web-app/src/App.tsx` 中将 `SettingsPanel`、`InterpretationPanel`、整副牌数据 `tarotData.ts`、以及解牌服务 `llm.ts` 全部改为延迟加载；只有在打开配置、展开解读或真正开始抽牌时才请求对应代码。
- **3D 舞台分离**：新增 `web-app/src/components/TarotScene.tsx`，把 `Canvas + Effects + TarotCard` 这组 Three.js 场景整体拆出主入口，避免首页框架和 3D 场景被打进同一个首包里。
- **验证结果**：本地 `npm run build` 通过。构建产物中主入口 `index` 已从约 1.2 MB 降到约 197 KB，而重的 3D 场景被拆到独立的 `TarotScene` chunk 中；当前仍有大 chunk 告警，但它已经不再阻塞首页框架首达。
